/**
 * Woocommerce helpers
 * TODO: do i realy need this?
 */
export type WoocommerceSettings = {
	images: {
		productpage: {
			width: number;
			height: number;
		};
		taxonomy_pages: {
			width: number;
			height: number;
		};
	};
};
